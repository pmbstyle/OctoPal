# BroodMind

MVP-0 skeleton for the BroodMind Queen + Workers platform.

## Quick start

1. Create `.env` from `.env.example` and fill in keys.
2. Install deps (prefer a virtual environment).
3. Run `broodmind start`.
